# 财务通用 Agent 最终用户手册

**版本：** V1.0  
**适用系统：** finance-general-agent  
**更新日期：** 2026-06-04

## 1. 产品概述

财务通用 Agent 是面向财务人员、业务审批人员和管理者的智能财务助手。用户可以通过网页或钉钉应用入口，用自然语言查询财务数据、创建业务单据、生成凭证和报表，并查看 Agent 调用过的工具及处理结果。

系统当前提供：

- 财务数据查询与分析。
- 发票识别、查验、查重和入账辅助。
- 报销单创建、查询和智能审批。
- 凭证创建、查询、科目推荐和自动审核。
- 资产负债表、利润表、现金流量表生成。
- 应收应付、催收、付款计划和往来对账。
- 预算、税务、银行对账、合规风控和月结检查。
- 资金预测、合同资产、数据集成状态查询。
- 调用链与审计日志查询。

> 当前默认环境加载演示数据。涉及付款、纳税申报、正式记账和审批流的结果，应由有权限的财务人员复核后执行。

## 2. 使用角色

### 2.1 普通业务用户

- 创建和查询本人报销单。
- 上传发票并查看识别结果。
- 查询可用预算和审批状态。

### 2.2 财务人员

- 查询发票、凭证、应收应付和银行流水。
- 生成凭证、财务报表、税务申报草稿。
- 执行对账、月结检查和异常分析。

### 2.3 财务管理者

- 查看财务驾驶舱和关键指标。
- 查看预算执行、资金预测和合规风险。
- 获取催收、付款、融资和经营改进建议。

### 2.4 系统管理员与开发人员

- 启动和维护各服务。
- 通过 MCP Gateway 或财务 MCP 调用工具。
- 接入真实 Dubbo、MySQL、Redis、钉钉和智谱服务。

## 3. 快速开始

### 3.1 访问系统

服务启动后，在浏览器访问：

```text
http://localhost:5173
```

如果前端端口被占用，Vite 可能自动使用 `5174`，请以终端输出为准。

### 3.2 页面入口

| 页面 | 地址 | 主要用途 |
|---|---|---|
| 对话 | `/chat` | 使用自然语言查询和执行业务 |
| 驾驶舱 | `/cockpit` | 查看财务指标、趋势和风险 |
| 发票 | `/invoice` | 上传发票并执行 OCR 到凭证流程 |
| 报表 | `/report` | 查看资产负债表、利润表和现金流量表 |

### 3.3 发起第一次对话

进入“对话”页面，在输入框中输入：

```text
查询2026-05应收账款汇总
```

Agent 会自动选择相应工具，并展示查询结果和本次调用链。

### 3.4 输入指令的建议格式

为了让 Agent 更准确地处理任务，建议指令中包含：

```text
动作 + 业务对象 + 时间范围/编号 + 金额或筛选条件 + 期望输出
```

示例：

```text
生成2026-05资产负债表，并检查是否平衡
查询发票INV-202605-003，检查真伪、查重和抵扣状态
为E1002创建一张销售部3600元差旅报销单并提交
```

## 4. 页面使用说明

### 4.1 对话页面

对话页面是系统的主要操作入口。

可使用的交互方式：

- 在聊天输入框中输入自然语言指令。
- 点击“查应收、记账、查发票、出报表、费用分析、算税”等快捷操作。
- 使用语音输入，将语音转为文本后发送。
- 上传文件或发票图片。
- 查看历史会话、新建会话、切换或删除会话。
- 复制 Agent 回复，或对上一条指令重新生成。
- 查看本轮调用过的 MCP 工具、处理状态和耗时。

### 4.2 财务驾驶舱

驾驶舱用于查看关键财务指标、图表、趋势和风险提示。

使用方式：

1. 打开“驾驶舱”页面。
2. 查看应收、回款、预算、利润等指标。
3. 点击图表发起关联追问。
4. 双击图表进行下钻分析。
5. 使用自动刷新获取最新结果。

### 4.3 发票页面

发票页面支持上传发票图片，并自动执行 OCR 识别与凭证生成。

使用方式：

1. 打开“发票”页面。
2. 拖拽、拍照或选择发票图片。
3. 等待系统完成识别。
4. 核对发票号码、金额、税额和购销方信息。
5. 查看自动生成的凭证结果。
6. 正式入账前由财务人员复核科目和金额。

### 4.4 报表页面

报表页面用于查看三大财务报表。

可查看：

- 资产负债表及平衡校验。
- 利润表收入、成本和利润。
- 现金流量表经营、投资和筹资现金流。

## 5. 功能与指令示例

### 5.1 应收应付与往来管理

支持应收看板、账龄分析、催收建议、催收函、付款计划和往来对账。

可直接输入：

```text
查询2026-05应收账款汇总
查询截至2026-05-31的应收账龄分析
为客户C1001生成催收建议和催收措辞
生成C类逾期35天客户应收催收计划和催收函
优化上海数科服务有限公司42000元应付付款计划，考虑提前付款折扣
对浙江智造设备有限公司做2026-05应收往来对账，生成余额确认函
```

### 5.2 凭证管理

支持科目推荐、凭证创建、台账查询、凭证明细和自动审核。

可直接输入：

```text
给上海数科服务有限公司42000元软件服务费推荐会计科目和凭证预览
帮我生成一张1200元办公费记账凭证
查询2026-05凭证台账
查看凭证V-20260531-0001明细
审核一张采购异常凭证，检查三单匹配、借贷平衡和岗位分离
```

凭证审核会检查：

- 借贷是否平衡。
- 科目使用是否合理。
- 发票、订单和入库单是否匹配。
- 是否跨期或重复入账。
- 制单人与审核人是否符合岗位分离要求。

### 5.3 发票管理

支持发票录入、台账查询、标准查验、查重、增强验真、抵扣和归档检查。

可直接输入：

```text
查询发票台账
对发票INV-202605-001做标准查验
查重发票INV-202605-001，检查是否重复报销或重复入账
查验发票INV-202605-003真伪、查重和抵扣状态
录入一张杭州差旅服务有限公司3600元发票，发票号INV-202606-009，并自动验真
上传识别一张发票并生成凭证
```

### 5.4 费用报销

支持报销单创建、查询、预算余额查询和智能审批。

可直接输入：

```text
为E1002创建一张销售部3600元差旅报销单并提交，包含发票和附件
查询E1002的待审批报销单
查询销售部2026-05费用预算余额
审批销售部3200元差旅报销
审批一张8600元采购异常报销单，检查预算、发票和三单匹配
```

智能审批会检查：

- 费用标准和超标金额。
- 发票是否缺失、验真失败或重复使用。
- 报销金额与发票金额是否一致。
- 采购订单、入库单和发票三单匹配。
- 部门预算是否充足。
- 是否需要追加财务经理或分管领导审批。

审批结果会回写报销单状态：

- `APPROVED_ROUTE_READY`：校验通过，可进入审批流。
- `NEED_REVIEW`：存在黄色风险，需要人工复核。
- `REJECTED`：存在红色风险，已自动拦截。

### 5.5 财务报表

可直接输入：

```text
生成2026-05资产负债表，并检查是否平衡
生成2026-05利润表
生成2026-05现金流量表
生成本月财务报表
```

系统根据数据库中的科目、凭证和凭证分录进行 SQL 聚合。

### 5.6 财务分析与预算

支持财务指标、趋势、异常检测、差异归因和预算管控。

可直接输入：

```text
计算2026-05财务比率和健康度
分析2026-01到2026-05营业收入趋势
检测2026-05财务异常项
2026-05销售费用为什么超了，帮我做差异归因和行动计划
评估销售部98000元差旅预算占用，检查预算执行率和超支预警
```

### 5.7 税务管理与政策查询

支持税额计算、申报表草稿和网页政策摘要。

可直接输入：

```text
帮我计算128.65万元增值税税额
生成2026年5月增值税申报表
增值税税率政策是什么
读取指定税务政策网页并总结关键条款
```

> 税务政策和申报表结果仅供辅助，正式申报前必须按当前有效政策和主管税务机关口径复核。

### 5.8 银行对账与资金管理

支持银行流水、未达账、银行对账、现金流预测和融资建议。

可直接输入：

```text
查询2026-05银行流水
查询基本户未达银行流水
执行2026-05银行对账
预测未来3个月现金流和资金缺口
给出账户调拨和融资建议
```

### 5.9 合规、月结与审计

可直接输入：

```text
评估2026-05合规风控风险
执行2026-05月结检查，生成关账清单和阻断项
查询本会话的调用审计日志
查询失败的MCP工具调用记录
```

### 5.10 合同资产与数据集成

可直接输入：

```text
查询合同收付款节点和固定资产折旧
查询资产盘点异常和无形资产摊销提醒
查询ERP银企直连税务OA数据集成状态
查询失败的ETL任务和数据质量问题
```

## 6. 钉钉入口使用

本地模拟钉钉入口：

```text
http://localhost:5173/chat?channel=dingtalk&corpId=demo-corp&userId=zhangsan&userName=张三
```

进入后，系统会保存当前钉钉用户上下文，并将渠道、企业、用户等信息写入调用审计日志。

真实钉钉环境还需要管理员完成：

- 配置 `VITE_DINGTALK_CORP_ID`。
- 在钉钉容器中获取免登授权码。
- 在后端将授权码兑换为真实用户身份和权限。

## 7. 结果说明与安全提示

### 7.1 结果展示

Agent 可能以以下形式返回结果：

- 文本说明。
- 数据表格。
- 指标卡片。
- ECharts 图表。
- MCP 调用链和处理耗时。

### 7.2 数据写入

以下操作会写入数据库：

- 创建凭证。
- 录入发票。
- 创建报销单。
- 报销审批状态更新。
- 写入 Agent 调用审计日志。

### 7.3 风险控制

- 付款、报税、正式记账和审批结果必须人工复核。
- 不要在聊天框输入密码、密钥或完整银行卡信息。
- 发现数据异常时，应查看调用链并联系管理员。
- 无真实智谱密钥时，多模态能力会使用确定性本地结果。

## 8. 常见问题

### 8.1 页面无法访问

确认前端服务已启动，并尝试：

```text
http://localhost:5173
http://localhost:5174
```

### 8.2 Agent 提示服务不可用

可能是某个 MCP 服务未启动。管理员应检查 Gateway 工具清单和不可用服务：

```bash
curl -s http://localhost:9000/mcp \
  -H 'Content-Type: application/json' \
  -d '{"jsonrpc":"2.0","id":1,"method":"tools/list","params":{}}'
```

### 8.3 指令没有调用预期功能

补充明确的业务关键词、日期、编号和动作。例如不要只输入“查一下”，应输入：

```text
查询2026-05销售部待审批报销单
```

### 8.4 发票识别结果不准确

- 使用清晰、完整、无遮挡的图片。
- 核对发票号码、金额和税额。
- 配置 `Z_AI_API_KEY` 后使用真实视觉模型。
- 正式入账前必须人工复核。

### 8.5 报表数据为什么变化

报表由数据库凭证分录实时聚合。创建新凭证后，相应期间的报表结果可能变化。

## 9. 管理员启动指南

### 9.1 环境要求

- Java 8 或兼容环境。
- Maven。
- Node.js 与 npm。
- 可选：MySQL、Redis、Playwright Chromium、智谱 API Key。

### 9.2 启动顺序

在仓库根目录执行：

```bash
mvn -q -DskipTests package
java -jar finance-service/finance-starter/target/finance-starter-0.0.1-SNAPSHOT.jar
```

另开终端启动财务 MCP：

```bash
mvn -pl mcp/finance-mcp spring-boot:run
```

分别启动其他 MCP：

```bash
cd mcp/zai-mcp-server && npm install && npm run dev
cd mcp/web-reader-server && npm install && npm run dev
cd mcp/integrated-browser-server && npm install && npm run dev
cd mcp/mcp-gateway && npm install && npm run dev
```

最后启动前端：

```bash
cd finance-web
npm install
npm run dev
```

### 9.3 默认服务端口

| 服务 | 默认端口 | 用途 |
|---|---:|---|
| 财务 Dubbo Provider | 20880 | 后端财务服务 |
| Finance MCP | 8086 | 财务 Dubbo MCP |
| ZAI MCP | 8088 | OCR 与多模态 |
| Web Reader MCP | 8089 | 网页与政策读取 |
| Browser MCP | 8090 | Playwright 浏览器自动化 |
| MCP Gateway | 9000 | MCP 聚合与 Agent Chat |
| Finance Web | 5173 | 用户 Web 页面 |

### 9.4 启用真实模型

启用 Gateway 的 GLM Function Calling：

```bash
cd mcp/mcp-gateway
AGENT_MODE=llm GLM_API_KEY=your-key npm run dev
```

启用真实多模态和智谱 web-reader：

```bash
Z_AI_API_KEY=your-key npm run dev
```

### 9.5 生产数据源

默认使用 H2 演示数据库。切换 MySQL 和 Redis：

```bash
SPRING_PROFILES_ACTIVE=production \
FINANCE_MYSQL_URL='jdbc:mysql://127.0.0.1:3306/finance_demo?serverTimezone=Asia/Shanghai' \
FINANCE_MYSQL_USERNAME=finance \
FINANCE_MYSQL_PASSWORD=finance \
FINANCE_REDIS_HOST=127.0.0.1 \
java -jar finance-service/finance-starter/target/finance-starter-0.0.1-SNAPSHOT.jar
```

### 9.6 健康检查与测试

```bash
bash mcp/scripts/smoke-test.sh
bash mcp/scripts/browser-smoke-test.sh
bash mcp/scripts/web-reader-smoke-test.sh
bash mcp/scripts/finance-persistence-smoke-test.sh
```

正常情况下 Gateway 聚合 76 个工具，且不可用服务数量为 0。

## 10. MCP 与接口调用附录

### 10.1 通过 Agent Chat 调用

```bash
curl -s http://localhost:9000/agent/chat \
  -H 'Content-Type: application/json' \
  -d '{"message":"生成2026-05资产负债表"}'
```

### 10.2 查询 Gateway 工具列表

```bash
curl -s http://localhost:9000/mcp \
  -H 'Content-Type: application/json' \
  -d '{"jsonrpc":"2.0","id":1,"method":"tools/list","params":{}}'
```

### 10.3 直接调用财务 MCP

```bash
curl -s http://localhost:8086/mcp \
  -H 'Content-Type: application/json' \
  -d @mcp/finance-mcp/examples/query-ar-dashboard.sample.json
```

### 10.4 MCP 模块组成

| 模块 | 工具数 | 说明 |
|---|---:|---|
| Finance MCP | 45 | 财务语义工具与 Dubbo 泛化调用 |
| ZAI MCP | 8 | OCR、图像、图表和 UI 分析 |
| Web Reader MCP | 1 | 网页和政策内容读取 |
| Browser MCP | 22 | 浏览器导航、表单、读取和会话管理 |
| Gateway 聚合 | 76 | 统一工具入口 |

## 11. 当前联调边界

以下能力已预留接口，但在正式上线前仍需企业环境联调：

- 替换为企业真实 Dubbo 财务接口、DTO 和权限体系。
- 配置真实智谱模型密钥并验证调用额度。
- 接入钉钉服务端免登和用户权限。
- 验证生产 MySQL、Redis 和数据库初始化策略。
- 对付款、报税、审批和正式凭证入账增加组织级授权与复核流程。

